services:
  funnel:
    build: .
    ports:
      - '3000:3000'
    environment:
      DATA_DIR: /data
      BOOTSTRAP_CONFIG: funnel.v1.json
      EXPERIMENT_SALT: funnel-runtime
    volumes:
      - funnel-data:/data
    restart: unless-stopped

volumes:
  funnel-data:
